import { DefaultTemplate } from "../../templates";

export default () => {
  return (
    <DefaultTemplate headTitle="Create battle">
      <div>Create battle form</div>
    </DefaultTemplate>
  );
};
